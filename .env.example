# ── Base de datos (Railway provee estos valores) ──────────────────────────
DB_HOST=tu-host.railway.app
DB_PORT=3306
DB_USER=root
DB_PASS=tu-password-railway
DB_NAME=alvarez_fastfood

# ── Aplicación ────────────────────────────────────────────────────────────
# URL pública del frontend en Render (sin barra final)
FRONTEND_URL=https://tu-app.onrender.com

# Puerto que usa uvicorn (Render lo inyecta automáticamente)
PORT=8000
